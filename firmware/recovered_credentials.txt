rootfs /etc/passwd DES hash and recovered value (historical reference only):
04h6XLo9zAfEM:sl.x.

Note: the live camera's rootfs /etc/shadow root password is empty; debug.sh
sets a real password at boot from a hash on configfs. This value is not used
for development. See AGENTS.md.
